import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    div: {
        width: 200,
        height: 170,
        alignItems: 'center',
        justifyContent: 'center',
    },
    num: {
        fontSize: 80,
        color: '#11aabb',
    },

    more: {

        backgroundColor: '#1f5',
        height: 60,
        borderRadius: 30,
        alignItems: 'center',
        justifyContent: 'center',
        width: 180,
        height: 50

    },

    less: {
        margin: 20,
        backgroundColor: '#ff1441',
        height: 60,
        borderRadius: 30,
        alignItems: 'center',
        justifyContent: 'center',
        width: 180,
        height: 50,
    },

    textb: {
        color: "#fff",
        fontSize: 30
    },

    center: {
        alignItems: 'center',
        justifyContent: 'center',

    }
})

export default styles